<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.froyo.ridekaro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_rapido_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_rapido_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RideKaro">
        <activity android:name=".views.navDrawerFragments.RiderArrived"></activity>
        <activity android:name=".views.navDrawerFragments.GetVaccinated" />
        <activity android:name=".views.RiderComing" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.Temp" />
        <activity android:name=".views.Book_Vaccine_Activity" />
        <activity android:name=".views.StopwatchActivity" />
        <activity android:name=".views.InvitefriendsActivity" />
        <activity android:name=".views.OTPSecondActivity" />
        <activity android:name=".views.OTPValidation" />
        <activity android:name=".views.HomeActivity" />
        <activity android:name=".views.LanguageScreenActivity" />
        <activity android:name=".views.SecondScreenActivity" />
        <activity android:name=".views.FirstScreenActivity">

        </activity>

        <service android:name=".NotificationService" />
    </application>

</manifest>